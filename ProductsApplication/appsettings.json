{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "ApiProjectConfig": {

    "ConnectionString": "Server=tcp:172.20.181.247,1600;Database=Test;User ID=sa;Password=ad13!min;",
    "TextLogDirectory": "logs",

    "ServerSideCacheExpirationInSeconds": 1800, // If the resource is updated/deleted, then the cache will be cleared regardless of time.
    "ClientSideCacheExpirationInSeconds": 180, // The client can force the Cache refresh before the time expires.

    "ErrorLogDestination": "Console", // Allowed values: TextFile/Database/Console

    "IsApiTraceEnabled": true,
    "ApiTraceDestination": "Console", // Allowed values: TextFile/Database/Console

    "IsDbQueryTraceEnabled": false,
    "DbQueryTraceDestination": "Console" // Allowed values: TextFile/Console; Database not available
  }
}
